TODO:
  - check to add locks to avoid race conditions
  - set signal properly - Maybe works.  Need to test
  - restart only when necessary
  - improve UI
  - check if config has been changed
  - handle stdin/stdout for multiple instances of process
  - make sure we handle duplicate process names
  - handle other args:
    - umask
    - workingdir
    - autostart
    - autorestart
    - startretries
    - starttime //set to time
    - stoptime
    - stdin
    - stdout
    - stderr
    - env
